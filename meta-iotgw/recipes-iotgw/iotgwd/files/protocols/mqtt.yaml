connectors:
  mqtt_local:
    type: mqtt
    params:
      host: "localhost"
      port: 1883
      client_id: "iotgw-rpi4-64"
      clean_session: true
      keepalive_s: 40
      qos: 1
      retain: true
      # username: "user"
      # password: "pass"
      # tls:
      #   enabled: true
      #   ca_file: "/etc/ssl/certs/ca-certificates.crt"
      #   cert_file: "/etc/iotgw/certs/client.crt"
      #   key_file: "/etc/iotgw/certs/client.key"
      # topics:
      #   - topic: "cmd/#"
      #     qos: 1
